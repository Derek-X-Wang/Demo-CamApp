# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CamApp' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CamApp
  pod 'RxSwift',    '~> 4.0'
  pod 'RxCocoa',    '~> 4.0'
  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  pod 'Firebase/Database'
  pod 'Firebase/Storage'
  pod 'NVActivityIndicatorView'
  pod "BAFluidView"
  
  target 'CamAppTests' do
    inherit! :search_paths
    # Pods for testing
    pod 'RxBlocking', '~> 4.0'
    pod 'RxTest',     '~> 4.0'
  end

  target 'CamAppUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
